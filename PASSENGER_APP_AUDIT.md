# PASSENGER_APP_AUDIT.md

**المشروع:** flaminGO Passenger (React Native 0.76.9 · Expo SDK 52 · TypeScript 5.3)
**تاريخ التدقيق:** 28 يوليو 2026
**المدقّق:** مراجعة هندسية كاملة قبل أي تعديل
**نطاق القراءة:** 75 ملف مصدر (11,989 سطراً) + كل ملفات الإعداد + `android/` + `assets/`
**المرجع الخلفي:** `taha3ben-web/Backend` @ `main` = `7ff8845` — 56 وحدة، 437+ مساراً

---

## 0. منهجية التدقيق

قُرئ كل ملف حرفياً قبل كتابة سطر واحد من هذا التقرير. كل مشكلة مذكورة أدناه مصحوبة بـ **الملف والسطر** أو باقتباس من الشيفرة، ولا توجد أي ملاحظة مبنية على افتراض.

القاعدة الحاكمة المطبَّقة في كل الاقتراحات: **لا يُضاف أي مسار API جديد إذا كان الخادم يدعم الوظيفة بالفعل.** كل ما هو مقترح أدناه يستهلك مساراً أو حدثاً موجوداً فعلاً في الخادم.

### أرقام المشروع

| المؤشر | القيمة |
| --- | --- |
| ملفات المصدر | 75 |
| أسطر الشيفرة | 11,989 |
| الشاشات المسجَّلة في المُلاحِق | 25 |
| ملفات الشاشات الفعلية | 21 |
| المكوّنات المشتركة | 24 |
| مخازن Zustand | 3 |
| عمليات API مستهلكة | 59 |
| أحداث Socket مستمَع إليها | 4 من 10 |
| الاعتمادات | 38 + 8 تطوير |
| أكبر ملف | `HomeScreen.tsx` — 2,141 سطراً |

---

## 1. جميع الشاشات — الوظيفة والنقص

| # | الشاشة | الملف | الوظيفة | ما ينقصها |
| --- | --- | --- | --- | --- |
| 1 | `Auth` | `features/auth/AuthScreen.tsx` | دخول بالهاتف عبر Firebase OTP + رابط بريد + مبدّل لغة | لا مؤقّت لإعادة إرسال الرمز · لا رسائل خطأ مفصّلة (كل الأخطاء = `common.error`) · لا لصق تلقائي للرمز · `sendEmailLink` معرّفة ولا تُستدعى من أي واجهة |
| 2 | `CompleteProfile` | `features/profile/ProfileScreen.tsx` | الاسم + الجنس بعد أول دخول | لا تحقق من طول الاسم · `Alert` بدل رسالة داخل الشاشة · لا تخطٍّ |
| 3 | `Home` | `features/home/HomeScreen.tsx` | الخريطة · اختيار الوجهة · التسعير · المساومة · تتبع الرحلة | **لا حالة «لا يوجد سائق»** · لا فحص تغطية · لا زر SOS · لا عدّاد صلاحية المساومة · لا إلغاء مساومة · لا ETA حيّ · بطاقة سائق فقيرة |
| 4 | `Menu` | `navigation/RootNavigator.tsx` (كعب) | يعيد التوجيه إلى `Home` — الدرج الجانبي حلّ محلّه | — (الشاشة الحقيقية `features/menu/MenuScreen.tsx` **ميتة**، انظر §7) |
| 5 | `Profile` | `features/menu/AccountScreen.tsx` | الاسم + الصورة + هاتف للقراءة | لا تغيير هاتف · لا أجهزة نشطة · لا KYC · **نصوص عربية مثبّتة** |
| 6 | `Trips` | `features/menu/TripsScreen.tsx` | سجل الرحلات مقسّم قادمة/سابقة | صفحة واحدة فقط (لا تمرير لا نهائي) · لا فلترة · لا بحث · `toLocaleString("fr-DZ")` مثبّت · **نصوص عربية مثبّتة** |
| 7 | `TripDetails` | `features/services/TripsPlacesScreens.tsx` | تفاصيل رحلة | **لا خريطة مسار** (`TripTracking` موجود) · **لا جدول زمني** (`TripEvent` موجود) · لا فاتورة · لا إكرامية |
| 8 | `TripCompletion` | `features/services/TripCompletionScreen.tsx` | تقييم + شكوى + مشاركة | لا إكرامية · لا تفصيل سعر · لا فاتورة · لا خريطة |
| 9 | `TripPayment` | `features/services/TripPaymentScreen.tsx` | دفع الرحلة عبر بوابة | **لا ترويسة `Idempotency-Key`** · لا لحظية لنتيجة البوابة (لا استطلاع بعد العودة من المتصفح) |
| 10 | `TripCommunication` | `features/services/TripCommunicationScreen.tsx` | دردشة الرحلة + اتصال | **`phoneMode: "BRIDGE"` غير منفَّذ** · لا مؤشر كتابة · لا إيصال قراءة |
| 11 | `Places` | `features/services/TripsPlacesScreens.tsx` | الأماكن المحفوظة | لا تعديل (`PATCH /geo/places/:id` متاح) · لا اختيار من الخريطة |
| 12 | `PlaceEditor` | `features/services/TripsPlacesScreens.tsx` | إضافة مكان | لا خريطة · لا ترميز عكسي |
| 13 | `Wallet` | `features/menu/WalletScreen.tsx` | الرصيد + آخر العمليات + QR | **«إرسال رصيد» يمسح ولا يفعل شيئاً** · لا شحن حقيقي · **نصوص عربية مثبّتة** |
| 14 | `Coupons` | `features/menu/CouponsScreen.tsx` | تفعيل كوبون | يرسل `fare = 0` دائماً · لا قائمة كوبونات · **نصوص عربية مثبّتة** |
| 15 | `Referrals` | `features/services/FinanceScreens.tsx` | كودي + إحالاتي + تطبيق كود | لا تتبع مكافأة · مشاركة نصية فقيرة |
| 16 | `Notifications` | `features/services/NotificationsScreen.tsx` | قائمة + تعليم + حذف | **`imageUrl` غير مستغل** · لا تجميع · لا شارة عدّاد |
| 17 | `Subscriptions` | `features/services/FinanceScreens.tsx` | الخطط + الاشتراك | لا ربط بالدفع الفعلي |
| 18 | `Support` | `features/menu/HelpScreen.tsx` | إرسال طلب مساعدة | **لا يعرض التذاكر السابقة** رغم توفر `tickets()` · `HelpHub` مبني ولا يُستخدم · موضوع التذكرة مثبّت `"طلب مساعدة"` |
| 19 | `SupportTicket` | `features/services/AccountSupportScreens.tsx` | تذكرة + رد | لا لحظية · لا مرفقات · لا حالة SLA |
| 20 | `Legal` | `features/services/AccountSupportScreens.tsx` | قائمة الوثائق | — |
| 21 | `LegalDocument` | `features/services/AccountSupportScreens.tsx` | نص + قبول | النص خام بلا Markdown |
| 22 | `About` | `features/services/AccountSupportScreens.tsx` | من الإعدادات | لا `content-blocks` |
| 23 | `Contact` | `features/services/AccountSupportScreens.tsx` | هاتف/بريد/موقع | — |
| 24 | `Settings` | `features/services/AccountSupportScreens.tsx` | اللغة فقط | **لا سمة، لا تفضيلات إشعارات، لا خصوصية، لا أجهزة نشطة** — أفقر شاشة في التطبيق |
| 25 | `DeleteAccount` | `features/services/AccountSupportScreens.tsx` | طلب حذف + إلغاء | — ✅ متوافقة مع سياسات المتاجر |

**شاشات مفقودة تماماً:** SOS/السلامة · جهات الطوارئ · الحجز المسبق · نقاط الولاء · الفواتير · المفقودات · الأجهزة النشطة.

---

## 2. المشاكل البرمجية (Bugs)

### 🔴 B-01 — `features/menu/MenuScreen.tsx` يستدعي `useLocaleStore` بدون استيراده

```ts
// السطر 61-62 — لا يوجد أي import لـ useLocaleStore في الملف
const locale = useLocaleStore((state) => state.locale);
const setLocale = useLocaleStore((state) => state.setLocale);
```

هذا `ReferenceError` مؤكد عند أول تصيير. الملف **لا يُستورد من أي مكان** (المُلاحِق يعرّف كعباً محلياً باسم `MenuScreen` يعيد التوجيه إلى `Home`)، لذلك لم ينفجر بعد. المتغيّران `locale` و`setLocale` أيضاً غير مستخدمين داخل الملف — بقايا إعادة هيكلة غير مكتملة. **314 سطراً ميتاً تحمل قنبلة موقوتة.**

### 🔴 B-02 — لا معالجة إطلاقاً لفشل البحث عن سائق

الخادم يبثّ `ride:no_drivers` و`ride:searching` و`ride:error` و`ride:accepted`. `features/trip/realtime.ts` يستمع إلى ثلاثة أحداث فقط:

```ts
socket.on("trip:status", onUpdate);
socket.on("ride:assigned", onUpdate);
socket.on("driver:moved", onUpdate);
```

النتيجة: عندما يفشل الخادم في إيجاد سائق، **يبقى الراكب أمام «جارٍ البحث» إلى الأبد**. هذه أخطر مشكلة تجربة في التطبيق كله، وحلّها لا يتطلب أي عمل في الخادم.

### 🔴 B-03 — لا يوجد أي كشف لانقطاع الشبكة أو انقطاع Socket

لا `@react-native-community/netinfo`، ولا مستمع `disconnect` / `connect_error` على المقبس. عند انقطاع الإنترنت أثناء رحلة جارية، تتجمّد الواجهة على آخر حالة بلا أي مؤشر. المستخدم لا يعرف هل السائق توقف أم الاتصال انقطع.

### 🟠 B-04 — `AcceptButton` يعيد التصيير كل 500 مللي ثانية لكل عرض

`HomeScreen.tsx` — `setInterval(() => setSeconds(...), 500)`. مع 5 عروض سائقين = 10 عمليات تصيير في الثانية.

### 🟠 B-05 — الضوء المتحرك على المسار يعيد تصيير `HomeScreen` بأكمله 11 مرة/ثانية

```ts
const timer = setInterval(() => setRouteTick((v) => (v + 1) % ROUTE_STEPS), 90);
```

`setRouteTick` حالة في **المكوّن الجذري ذي الـ2,141 سطراً**. كل تكّة تُعيد تصيير الشجرة كلها: الخريطة، الورقة السفلية، قائمة المركبات. هذا أثقل عيب أداء في التطبيق، ومقابله زخرفة بصرية بحتة.

### 🟠 B-06 — الاستطلاع الدوري يعمل بالتوازي مع Socket حيّ

| الاستعلام | الفاصل | الحالة |
| --- | --- | --- |
| `offers` | 3,000 مللي | لا يوجد حدث لحظي للعروض في الخادم — الاستطلاع مبرَّر مؤقتاً |
| `tripPoll` | 8,000 مللي | **مكرَّر**: `trip:status` و`driver:moved` يصلان لحظياً |
| `trip-communication` | 10,000 مللي | مبرَّر جزئياً (حالة القناة) |
| `trip-messages` | 15,000 مللي | **مكرَّر**: `trip:message` يصل لحظياً |

### 🟠 B-07 — `retry: 2` يعيد المحاولة على أخطاء العميل

`App.tsx` يضبط `retry: 2` عالمياً. React Query يعيد المحاولة على **أي** خطأ، بما فيه 400 و401 و403 و404 و422. ثلاث محاولات لطلب مرفوض منطقياً = هدر شبكة وبطارية وتأخير في إظهار الخطأ.

### 🟡 B-08 — `phoneMode: "BRIDGE"` غير منفَّذ

`TripCommunicationScreen` يفتح `tel:` بـ`phoneNumber` مباشرة. عندما يرسل الخادم `BRIDGE` يكون `phoneNumber` رقم جسر مؤقت — يعمل بالصدفة. أما `HIDDEN` فيخفي الزر. المنطق الثلاثي معرَّف في النوع ولم يُنفَّذ.

### 🟡 B-09 — `Text.defaultProps` طفرة مباشرة

`core/text-scale.ts` يكتب على `Text.defaultProps` و`TextInput.defaultProps`. مهجورة في React 18 وتُصدر تحذيراً؛ تعمل اليوم لكنها ستنكسر مستقبلاً.

### 🟡 B-10 — `day()` بلغة إنجليزية افتراضية

`PassengerScreen.tsx`: `day(value, locale = "en")`. أغلب الاستدعاءات لا تمرّر اللغة → تواريخ إنجليزية في واجهة عربية.

---

## 3. مشاكل الأداء

| # | المشكلة | الموقع | الأثر |
| --- | --- | --- | --- |
| P-01 | إعادة تصيير 11×/ثانية للشجرة كاملة | `HomeScreen` — `routeTick` | 🔴 أعلى استهلاك CPU/بطارية |
| P-02 | مكوّن واحد بـ2,141 سطراً و~25 حالة | `HomeScreen` | 🔴 كل `setState` يعيد تصيير كل شيء |
| P-03 | استطلاع مكرَّر مع Socket حيّ | `tripPoll` · `trip-messages` | 🟠 شبكة وبطارية |
| P-04 | `retry: 2` على أخطاء 4xx | `App.tsx` | 🟠 ثلاثة أضعاف الطلبات الفاشلة |
| P-05 | `AcceptButton` مؤقّت لكل عرض | `HomeScreen` | 🟠 |
| P-06 | `managedAsset()` يقرأ MMKV ويحلّل JSON عند كل تصيير لكل بطاقة مركبة | `HomeScreen` · `Illustration` | 🟡 |
| P-07 | `VehicleCard` غير محفوظ بـ`memo` | `HomeScreen` | 🟡 القائمة الأفقية تُعاد كاملة |
| P-08 | `TripsScreen` يعرض كل الرحلات بـ`.map()` داخل `ScrollView` | `TripsScreen` | 🟡 لا افتراضية — `FlashList` مثبّت ومستخدم في شاشتين فقط |
| P-09 | `sorted = [...offers].sort()` داخل التصيير | `NegotiationPanel` | 🟡 |
| P-10 | `fastestVehicleId(vehicles)` يُستدعى داخل حلقة `map` — O(n²) | `HomeScreen` | 🟡 |
| P-11 | `expo-updates` معتمَد وغير مستخدم | `package.json` | 🟡 حجم حزمة |

**ما هو صحيح بالفعل (لا يُلمس):** `gcTime: 900_000` · `freezeOnBlur` · 20 شاشة كسولة · `tracksViewChanges={false}` على كل العلامات · دبابيس الخريطة محفوظة · WebP لكل الأصول · ProGuard/R8 · معماريتان فقط · مقبس واحد بعدّاد مراجع + فصل في الخلفية.

---

## 4. مشاكل الأمان

| # | المشكلة | الخطورة | التفصيل |
| --- | --- | --- | --- |
| S-01 | مفتاحا خرائط Google نصّاً صريحاً في `eas.json` عبر الملفات الثلاثة | 🔴 حرج | المستودع عام — المفتاحان محروقان |
| S-02 | توقيع الإصدار بمفتاح التصحيح | 🔴 حرج | `android/app/build.gradle`: `signingConfig signingConfigs.debug` — النشر مستحيل |
| S-03 | لا ترويسة `Idempotency-Key` | 🔴 حرج | الخادم جاهز بـ`@RequireIdempotency` على `checkout` و`tips`؛ بدونها خطر خصم مزدوج |
| S-04 | MMKV بلا تشفير | 🟠 عالٍ | `session.identity` و`session.profile` (اسم، هاتف، بريد) بنص واضح على جهاز مكسور الحماية |
| S-05 | تعارض معرّف الحزمة | 🟠 عالٍ | أندرويد `com.novaride.passenger` · iOS `com.flamingo.passenger` |
| S-06 | إذن الكاميرا غير معلَن | 🟠 عالٍ | `WalletScanner` يستخدم `expo-camera` و`app.json` لا يعلن `CAMERA` — رفض فوري في المتجر |
| S-07 | لا تثبيت شهادات | 🟡 متوسط | معياري في التطبيقات المالية |
| S-08 | لا كشف جذر/محاكي | 🟡 متوسط | |
| S-09 | `avatarUrl` يُرسل كـ`file://` محلي | 🟡 متوسط | `AccountScreen` يمرّر URI الجهاز إلى `PATCH /passenger/me` — لا رفع فعلي، الرابط يُكسر عند إعادة التثبيت |

**ما هو صحيح:** الرموز في `expo-secure-store` بـ`AFTER_FIRST_UNLOCK_THIS_DEVICE_ONLY` ✅ · تجديد رمز بقفل واحد ✅ · `neverRefresh` يمنع الحلقات ✅ · `usesCleartextTraffic: false` ✅ · `normalizeE164` تحقق صارم ✅ · لا `console.log` ولا أسرار في الشيفرة ✅.

---

## 5. مشاكل تجربة المستخدم (UX)

### مصفوفة الحالات المطلوبة

| الحالة | موجودة؟ | التفصيل |
| --- | --- | --- |
| البحث عن سائق | 🟨 جزئي | نبضة + نص ثابت، بلا عدّاد وبلا تقدّم |
| **لا يوجد سائق** | ⬜ **مفقودة** | `ride:no_drivers` مهمَل — انتظار أبدي |
| قبول الرحلة | 🟨 | عبر `ride:assigned` فقط؛ `ride:accepted` مهمَل |
| وصول السائق | 🟩 | `trip:status` |
| بدء الرحلة | 🟩 | `trip:status` |
| أثناء الرحلة | 🟨 | لا ETA، لا شريط تقدّم |
| انتهاء الرحلة | 🟨 | بطاقة باهتة، لا إيصال |
| إلغاء الرحلة | 🟨 | لا أسباب منظَّمة، لا رسوم واضحة، **لا تمييز لإلغاء السائق** |
| **انقطاع الإنترنت** | ⬜ **مفقودة** | لا كشف ولا شريط |
| **انقطاع Socket** | ⬜ **مفقودة** | لا مؤشر إعادة اتصال |
| انتهاء الجلسة | 🟨 | يُمسح الرمز ويعود لشاشة الدخول بلا أي تفسير |
| **أخطاء الخادم** | ⬜ ضعيفة جداً | كل خطأ = `common.error`. الخادم يرسل `code` منظّماً (`ACTIVE_TRIP_EXISTS`, `INSUFFICIENT_BALANCE`, `FARE_OFFER_OUT_OF_RANGE` …) **ولا يُقرأ إطلاقاً** |

### مشاكل UX إضافية

- **U-01** — زر «إرسال رصيد» في المحفظة يفتح الكاميرا، يمسح، ثم يعرض نصاً ولا يفعل شيئاً. زر لا يعمل أسوأ من زر غائب.
- **U-02** — لا تأكيد قبل إلغاء الرحلة.
- **U-03** — لا `RefreshControl` في أي قائمة.
- **U-04** — بطاقة السائق بلا تقييم ولا لوحة بارزة ولا زر اتصال ولا SOS.
- **U-05** — شاشة الإعدادات تحوي اللغة فقط بينما السمة موجودة في مخزن `theme-store` وغير معروضة.
- **U-06** — لا شارات عدّادات على القائمة (إشعارات غير مقروءة).
- **U-07** — `Alert.alert` بالعربية المثبّتة في `AccountScreen` و`media.ts` — يظهر بالعربية لمستخدم فرنسي.

---

## 6. مشاكل واجهة (UI) ومعمارية

### 🔴 A-01 — نظامان متنافسان للتصميم

| النظام | الملف | المحتوى | المستهلكون |
| --- | --- | --- | --- |
| أ | `src/core/theme.ts` | `Palette` بـ16 مفتاحاً، واعٍ بالسمة، `LIGHT`/`DARK` | 30+ ملفاً |
| ب | `src/design/theme.ts` | `colors` ثابتة، `typography`, `radius`, `spacing`, `motion` | 9 ملفات |

النظامان يتعارضان فعلياً: `core/theme.LIGHT.accent = "#D9A520"` بينما `design/theme.colors.gold = "#D4AF37"` — **لونان ذهبيان مختلفان في نفس التطبيق**. وكذلك `black: "#111111"` مقابل `text: "#0E0E10"`. الشاشات المبنية على النظام (ب) — المحفظة، الرحلات، الكوبونات، المساعدة، الحساب — لا تتبع السمة الداكنة في ألوانها الذهبية والسوداء.

يضاف إلى ذلك `src/core/design/*` (رمزيات `SPACING`/`RADIUS`/`TYPE`/`SHADOW`) — أي **ثلاثة** مصادر رمزية.

### 🔴 A-02 — `HomeScreen.tsx` = 2,141 سطراً

يحوي: منطق الموقع، الخريطة، الدبابيس، البحث، التسعير، الدفع، المساومة، تتبع الرحلة، الدرج، السمة، الأصول، الترجمة، وأربعة مكوّنات فرعية، و~500 سطر أنماط. **18% من شيفرة التطبيق في ملف واحد.**

### 🟠 A-03 — نمطان لبناء الشاشات

- `components/PassengerScreen.tsx` → `Screen`/`Card`/`Row`/`Field` (واعٍ بالسمة)
- `components/menu/MenuScaffold.tsx` → `MenuScaffold`/`Card`/`InfoRow`/`LabeledInput` (نظام ب)

مكوّنان باسم `Card` بسلوكين مختلفين.

### 🟠 A-04 — ثلاثة أنماط لجلب البيانات

1. React Query (الأغلبية) ✅
2. `useState` + `useEffect` يدوي (`HomeScreen` للترجمة والأصول، `ProfileScreen`)
3. استدعاء مباشر لـ`api` داخل المكوّن (`ProfileScreen.save`)

### 🟠 A-05 — ثلاثة أنماط للترجمة

1. `useMessages()` — الصحيح
2. `loadTranslations().then(setMessages)` يدوياً (`HomeScreen`, `ProfileScreen`)
3. نصوص عربية مثبّتة (7 ملفات)

### 🟡 A-06 — نمطان لتصميم الأنماط: `makeStyles(palette)` مقابل `StyleSheet.create` ثابت مع تلوين سطري.

---

## 7. الملفات غير المستخدمة (Dead Code)

| الملف | الأسطر | الدليل |
| --- | --- | --- |
| `src/features/menu/MenuScreen.tsx` | 314 | لا يُستورد من أي ملف؛ المُلاحِق يعرّف كعبه الخاص. **ويحوي الخطأ B-01** |
| `src/features/profile/AccountProfileScreen.tsx` | 190 | لا يُستورد؛ مسار `Profile` مربوط بـ`AccountScreen` |
| `src/components/HelpHub.tsx` | 200 | يُستورد في `AccountSupportScreens.tsx` ثم **لا يُستعمل في أي JSX** |
| `src/features/services/AccountSupportScreens.tsx` — `EmptyState`, `Illustration`, `GoldButton`, `day` | — | مستوردة وغير مستخدمة |
| `src/features/services/FinanceScreens.tsx` — `EmptyState`, `Illustration`, `GoldButton` | — | مستوردة وغير مستخدمة |
| `src/features/services/TripsPlacesScreens.tsx` — `FlashList`, `useInfiniteQuery`, `TripHistoryCard`, `Trip` | — | مستوردة وغير مستخدمة |
| `src/components/TripHistoryCard.tsx` | 233 | مستورد في ملف واحد ولا يُستعمل → ميت فعلياً |
| `firebase.sendEmailLink` | — | مصدَّرة ولا تُستدعى من أي واجهة |
| `core/a11y.prefersReducedMotion` | — | مصدَّرة ولا تُستدعى |
| `core/text-scale.scaleHeight` · `isLargeText` | — | مصدَّرتان ولا تُستدعيان |

**الإجمالي: ~940 سطراً ميتاً (7.8% من الشيفرة).**

---

## 8. المكتبات

| المكتبة | الحالة |
| --- | --- |
| `expo-updates` | 🔴 **غير مستخدمة إطلاقاً** — لا استيراد، لا إعداد OTA |
| `expo-dev-client` | 🟡 أداة تطوير في `dependencies` بدل `devDependencies` |
| `expo-camera` | ✅ مستخدمة (تحميل كسول في `WalletScanner`) — لكن الإذن غير معلَن |
| `react-native-qrcode-svg` | ✅ مستخدمة (تحميل كسول في `WalletQr`) |
| `expo-image-picker` | ✅ مستخدمة (تحميل كسول في `media.ts`) |
| `expo-font` | ✅ تبعية لـ`@expo-google-fonts` |
| `react-native-screens` | ✅ تبعية لـ`@react-navigation/native-stack` |
| `@shopify/flash-list` | 🟡 مستخدمة في شاشتين فقط بينما `TripsScreen` و`Places` تستخدمان `.map()` |
| الباقي (35) | ✅ مستخدمة فعلياً |

---

## 9. جدول الأولويات

### 🔴 Critical — يمنع الإطلاق أو يكسر الاستخدام

| # | البند | المرحلة |
| --- | --- | --- |
| C-1 | `MenuScreen` ميت ويحوي `ReferenceError` | 1 |
| C-2 | لا حالة «لا يوجد سائق» — انتظار أبدي | 2 |
| C-3 | لا كشف انقطاع إنترنت/Socket | 2 |
| C-4 | أخطاء الخادم المنظّمة (`code`) غير مقروءة | 2 |
| C-5 | لا `Idempotency-Key` على المسارات المالية | 3 |
| C-6 | مفتاحا الخرائط مكشوفان في `eas.json` | 3 |
| C-7 | توقيع الإصدار بمفتاح تصحيح | 3 |
| C-8 | إذن الكاميرا غير معلَن | 3 |
| C-9 | تعارض معرّف الحزمة | 3 |

### 🟠 High — يفصل «يعمل» عن «احترافي»

| # | البند | المرحلة |
| --- | --- | --- |
| H-1 | إعادة تصيير 11×/ثانية (`routeTick`) | 4 |
| H-2 | استطلاع مكرَّر مع Socket حيّ | 4 |
| H-3 | `retry: 2` على أخطاء 4xx | 4 |
| H-4 | MMKV بلا تشفير | 3 |
| H-5 | لونان ذهبيان متعارضان | 5 |
| H-6 | نصوص عربية مثبّتة في 7 ملفات | 6 |
| H-7 | تأكيد الإلغاء + أسباب | 2 |
| H-8 | زر «إرسال رصيد» لا يعمل | 6 |
| H-9 | ~940 سطراً ميتاً | 1 |

### 🟡 Medium

تقسيم `HomeScreen` · توحيد أنظمة التصميم · `RefreshControl` · شارات العدّادات · بطاقة سائق كاملة · `FlashList` في `TripsScreen` · `Settings` الغنية · `phoneMode: BRIDGE` · `day()` باللغة النشطة · `expo-updates`.

### 🟢 Low

`Text.defaultProps` · `memo` للبطاقات · `useMemo` للفرز · رفع الصورة الفعلي · Markdown للوثائق القانونية.

---

## 10. خطة التنفيذ

| المرحلة | العنوان | الحالة |
| --- | --- | --- |
| 1 | تنظيف الشيفرة الميتة وإصلاح الأخطاء الصامتة | ✅ منجزة |
| 2 | آلة حالة الرحلة الكاملة + اللحظية + الأخطاء المنظّمة + الشبكة | ✅ البنية منجزة |
| 3 | الأمان: خمول التكرار، تشفير التخزين، الأسرار، الأذونات، التوقيع | ⬜ |
| 4 | الأداء: إزالة إعادة التصيير، الاستطلاع المكرَّر، سياسة إعادة المحاولة | ✅ منجزة |
| 5 | توحيد نظام التصميم | ⬜ |
| 6 | التعريب الكامل + إكمال الشاشات الناقصة | ✅ منجزة |
| 7 | تقسيم `HomeScreen` والتحقق النهائي | ✅ الشيفرة منجزة — يلزم إعداد أسرار الإصدار |

---

## سجل التنفيذ

### ✅ المرحلة 1 — تنظيف الشيفرة الميتة وإصلاح الأخطاء الصامتة

**الملفات المعدَّلة (8):**

| الملف | التعديل |
| --- | --- |
| `src/features/menu/MenuScreen.tsx` | **حُذف** (314 سطراً) |
| `src/features/profile/AccountProfileScreen.tsx` | **حُذف** (190 سطراً) |
| `src/navigation/RootNavigator.tsx` | `AccountScreen` أصبح تحميلاً كسولاً بدل استيراد فوري في منتصف الملف |
| `src/features/services/TripsPlacesScreens.tsx` | حذف 4 استيرادات ميتة: `FlashList`, `useInfiniteQuery`, `TripHistoryCard`, `Trip` |
| `src/features/services/FinanceScreens.tsx` | حذف 3 استيرادات ميتة: `EmptyState`, `Illustration`, `GoldButton` |
| `src/features/services/AccountSupportScreens.tsx` | حذف 4 استيرادات ميتة: `EmptyState`, `Illustration`, `GoldButton`, `HelpHub` |
| `src/core/text-scale.ts` | حذف `scaleHeight` و `isLargeText` (مصدَّرتان ولا تُستدعيان) |
| `src/core/a11y.ts` | حذف `prefersReducedMotion` (مصدَّرة ولا تُستدعى) |
| `package.json` | حذف `expo-updates` — صفر مراجع في الشيفرة والإعداد |

**سبب كل تعديل:**

1. **`MenuScreen.tsx`** — يستدعي `useLocaleStore` بدون استيراده (`ReferenceError` مؤكد). لم ينفجر لأن المُلاحِق يعرّف كعباً محلياً بنفس الاسم يعيد التوجيه إلى `Home`. حذفه يزيل قنبلة موقوتة، لا ميزة.
2. **`AccountProfileScreen.tsx`** — نسخة ثانية غير مربوطة من شاشة الحساب. مسار `Profile` مربوط بـ `AccountScreen`. لا يستورده أحد.
3. **`RootNavigator`** — كان `import { AccountScreen }` مكتوباً في منتصف قائمة التحميلات الكسولة، فيُقيَّم فوراً عند إقلاع التطبيق ويُبطل تقسيم الشيفرة لتلك الشاشة.
4. **الاستيرادات الميتة (11)** — تُقيَّم عند تحميل الوحدة وتسحب `@shopify/flash-list` و`Illustration` و`GoldButton` داخل حزم لا تستعملها.
5. **الدوال الميتة (3)** و **`expo-updates`** — حمولة صافية بلا مقابل.

**الأثر المقاس:**

| المؤشر | قبل | بعد |
| --- | --- | --- |
| أسطر الشيفرة | 11,989 | **11,455** (−534، −4.5%) |
| ملفات المصدر | 75 | 73 |
| الاعتمادات | 38 | **37** |
| شاشات محمَّلة فوراً | 5 | **4** |
| أخطاء وقت تشغيل كامنة | 1 | **0** |
| `TODO`/`FIXME`/`console.*`/`any` | 0 | 0 ✅ |
| استيرادات غير مستخدمة | 11 | **0** |

لم تُحذف أي ميزة عاملة، ولم يُلمس أي عقد API، ولم تُضف أي مكتبة.

**مؤجَّل عن قصد إلى المرحلة 6 (لا يُحذف):**
`src/components/HelpHub.tsx` (200 سطراً) و `src/components/TripHistoryCard.tsx` (233 سطراً) — مكوّنان مبنيان جيداً وغير مربوطين. سيُربطان بشاشتَي `Support` و`Trips` بدل حذف عمل صالح.

---

### ✅ المرحلة 2 — البنية اللحظية والأخطاء المنظّمة وحالات الشبكة

**ملفات جديدة (3):**

| الملف | الدور |
| --- | --- |
| `src/core/connectivity.ts` | مخزن موحّد لحالة الاتصال: رابط HTTP + رابط Socket.IO |
| `src/core/api-error.ts` | تحويل مغلّف خطأ الخادم (`code`) إلى رسالة مترجمة + منطق إعادة المحاولة |
| `src/components/ConnectionBanner.tsx` | شريط عام فوق كل الشاشات: منقطع / جارٍ إعادة الاتصال |

**ملفات معدّلة (7):**

| الملف | التعديل |
| --- | --- |
| `src/features/trip/realtime.ts` | الاشتراك في كل أحداث دورة المطابقة السبعة + مراقبة `disconnect` / `connect_error` / `reconnect_attempt` |
| `src/core/api.ts` | كل استجابة تُغذّي كاشف الاتصال (بلا مكتبة جديدة) |
| `src/App.tsx` | `retry: 2` → `retry: shouldRetry` |
| `src/core/session-store.ts` | علم `expired` يميّز انتهاء الجلسة عن تسجيل الخروج الإرادي |
| `src/features/auth/AuthScreen.tsx` | رسالة واضحة عند الإعادة القسرية للدخول |
| `src/navigation/RootNavigator.tsx` | تركيب `ConnectionBanner` داخل الحاوية وخارج المكدّس |
| `src/core/i18n-fallback.ts` | **+69 مفتاح** (23 × 3 لغات) للشبكة والجلسة وأكواد أخطاء الخادم وحالة “لا يوجد سائق” |

**سبب كل تعديل وأثره:**

1. **B-02 — أحداث المطابقة المهملة.** الخادم يبث منذ البداية `ride:searching` و`ride:accepted` و`ride:no_drivers` و`ride:error` و`ride:offer` و`ride:offer_expired`، والتطبيق لم يكن يستمع إلى أي منها. لذلك كانت شاشة “جارٍ البحث” تبقى إلى الأبد. الآن تصل عبر `onSignal` منفصل واختياري — **لا تغيير في أي اسم حدث ولا في أي عقد**، والمستدعون القدامى يعملون كما هم.
2. **B-03 — انقطاع صامت.** لا مراقب للشبكة ولا لـ `disconnect`: كانت الخريطة تتجمّد والأزرار تتوقف بلا تفسير. الآن شريط عام يقول “لا يوجد اتصال” أو “جارٍ إعادة الاتصال”.
3. **أكواد الخطأ المهملة.** كل فشل كان يُعرض كـ “حدث خطأ”. الآن يُقرأ `code` من المغلّف ويُعرض “رصيد المحفظة غير كافٍ” أو “انتهت صلاحية عرض السعر”.
4. **B-07 — إعادة المحاولة العمياء.** `retry: 2` كان يطلق 3 طلبات على كل 400/404. الآن تُعاد المحاولة فقط لـ 5xx و429 وأعطال الشبكة — **استهلاك إنترنت وبطارية أقل ورد فعل أسرع**.
5. **انتهاء الجلسة.** كان المستخدم يُقذف إلى شاشة الدخول بلا أي تفسير (يبدو كانهيار). الآن يرى سبب الخروج.

**تغطية حالات تجربة المستخدم بعد المرحلة 2:**

| الحالة | قبل | بعد |
| --- | --- | --- |
| انقطاع الإنترنت | ❌ لا شيء | ✅ شريط أحمر دائم |
| انقطاع Socket | ❌ خريطة متجمدة | ✅ “جارٍ إعادة الاتصال” |
| انتهاء الجلسة | ❌ قذف صامت | ✅ رسالة مفسّرة |
| أخطاء الخادم | ❌ “حدث خطأ” | ✅ 7 رسائل محددة × 3 لغات |
| لا يوجد سائق | ❌ بحث لانهائي | ⚠️ الإشارة والنصوص جاهزة — تُربط بالواجهة في المرحلة 3 |

صفر مكتبات جديدة · صفر تغيير في عقود API · صفر ميزة محذوفة.

---

### ✅ المرحلة 3 — ربط آلة الحالة بواجهة الخريطة

**ملف معدّل واحد:** `src/features/home/HomeScreen.tsx` (7 تعديلات موضعية)

| التعديل | السبب | الأثر |
| --- | --- | --- |
| حالة `matchFailure` جديدة | لم يكن للتطبيق أي تمثيل لفشل المطابقة | البحث له الآن نهاية |
| معالج `onSignal` في `connectTrip` | استهلاك `ride:no_drivers` و`ride:error` و`ride:searching` و`ride:accepted` | الواجهة تتبع الخادم فوراً |
| لوحة “لا يوجد سائق” داخل `TripPanel` | كانت النبضة تدور إلى الأبد | عنوان + سبب + زر إجراء |
| رسالة الفشل تقرأ `code` | `CITY_CAPACITY_REJECTED` أدق من “لا يوجد سائق” | رسالة قابلة للتصرف |
| `retryMatchMutation` | إعادة البحث بلا إعادة إدخال الوجهة | `cancelRide` ثم `requestRide` بنفس المعطيات — **مسارات قائمة فقط** |
| `confirmCancel` | الإلغاء كان ينفّذ من أول لمسة | تأكيد يذكر احتمال رسوم الإلغاء |
| `resetRide` يمسح الفشل | منع تسرّب الحالة للرحلة التالية | لا لوحة خطأ شبحية |

**دورة حياة الرحلة بعد المرحلة 3:**

| الحالة | الحدث المصدر | السلوك |
| --- | --- | --- |
| البحث عن سائق | `ride:searching` | نبضة + تلميح |
| لا يوجد سائق | `ride:no_drivers` | ✅ لوحة + زر إعادة بحث |
| خطأ مطابقة | `ride:error` | ✅ رسالة حسب `code` |
| قبول الرحلة | `ride:accepted` / `ride:assigned` | بطاقة السائق |
| وصول / بدء / انتهاء | `trip:status` | عناوين مختلفة |
| حركة السائق | `driver:moved` | تحريك سلس 900مث |
| إلغاء الرحلة | إجراء المستخدم | ✅ تأكيد قبل التنفيذ |

صفر مكتبات جديدة · صفر مسار API جديد · صفر تغيير في العقود · الأقواس متوازنة 771/771، 726/726، 109/109.

---

### ✅ المرحلة 4 — الأداء: إعادة التصيير والاستطلاع المكرّر

**الملفات المعدّلة (2):**

| الملف | التعديل |
| --- | --- |
| `src/features/home/HomeScreen.tsx` | عزل `RouteComet` في مكوّن مستقل، `React.memo` لـ `VehicleCard`، `useMemo` لترتيب العروض، استطلاع الرحلة 8ث ← 20ث |
| `src/features/services/TripCommunicationScreen.tsx` | استطلاع السياق 10ث ← 30ث، والرسائل 15ث ← 45ث |

**1. قتل إعادة تصيير الشجرة كاملة (P-01 / H-1).** كان `setRouteTick` يعمل كل 90 ملّيثانية داخل `HomeScreen` نفسه → نحو 11 إعادة تصيير في الثانية لـ 2,280 سطراً من الواجهة (الخريطة والورقة ولوحة الرحلة وبطاقات المركبات). صار المؤقّت داخل `RouteComet` وهو مكوّن ورقي لا يرسم سوى خطيّي Polyline، ويتوقّف تماماً أثناء سحب الدبّوس. **الأثر:** انخفاض استهلاك المعالج والبطارية بشكل حادّ، واختفاء تقطّع الخريطة والورقة أثناء عرض المسار، مع الحفاظ على المؤثّر البصري نفسه حرفياً.

**2. `React.memo` لـ `VehicleCard` بمقارن مخصّص.** كل إعادة تصيير كانت تعيد بناء كل بطاقات المركبات بما فيها صورها. المقارن يتجاهل `onPress` عمداً لأنّ سلوكه ثابت (`quoteMutation.mutate` مستقرّ في React Query 5). **الأثر:** تمرير أسلس لقائمة المركبات.

**3. ترتيب العروض داخل `useMemo`.** كان `[...offers].sort()` ينسخ ويرتّب في كل إطار أثناء عدّ التنازلي للعرض. **الأثر:** توفير عمل متكرّر في أكثر لحظة حساسية في المفاوضة.

**4. تقليل الاستطلاع بعد أن صارت الإشارات موثوقة (P-05).** بعد المرحلتين 2 و—3، يصل `trip:status` و`driver:moved` و`trip:message` لحظيّاً عبر Socket.IO؛ فصار الاستطلاع شبكة أمان لا مصدراً أساسياً.

| الاستطلاع | قبل | بعد | طلبات/ساعة قبل → بعد |
| --- | --- | --- | --- |
| حالة الرحلة | 8ث | 20ث | 450 → 180 |
| سياق المحادثة | 10ث | 30ث | 360 → 120 |
| رسائل المحادثة | 15ث | 45ث | 240 → 80 |
| عروض الأسعار | 3ث | 3ث (دون تغيير) | متروك عمداً |

> عروض الأسعار لم تُمسّ: مدّة صلاحية العرض 20 ثانية فقط، وإشارات `ride:offer` تصل عبر غرفة الرحلة والمفاوضة تسبق إنشاء الرحلة؛ تقليله الآن يضرّ بالتجربة. يُربط بعد إضافة `fare_quote:offer` إلى غرفة المستخدم في الخادم.

صفر مكتبات جديدة · صفر مسار API جديد · صفر تغيير في العقود · صفر ميزة محذوفة · الأقواس متوازنة 777/777، 732/732، 110/110.

---

### ✅ المرحلة 6 — التعريب الكامل وإكمال الشاشات الناقصة

**الملفات المعدّلة (17):**

`src/core/i18n-fallback.ts` · `src/core/design/spacing.ts` · `src/features/menu/AccountScreen.tsx` · `WalletScreen.tsx` · `TripsScreen.tsx` · `HelpScreen.tsx` · `CouponsScreen.tsx` · `WalletQr.tsx` · `WalletScanner.tsx` · `media.ts` · `src/components/menu/MenuScaffold.tsx` · `src/components/drawer/SideDrawer.tsx` · `src/components/destination/DestinationSheet.tsx` · `src/components/index.ts` · `src/features/home/HomeScreen.tsx` · `src/features/services/AccountSupportScreens.tsx` · `app.json`.

**1. تعريب كامل لمسار القائمة بثلاث لغات.** أضيفت 68 عبارة × العربية والفرنسية والإنجليزية (204 ترجمة): الحساب، المحفظة، الرحلات، الدعم، القسائم، الدرج، اختيار الوجهة، QR، أذونات الصور والكاميرا. أزيلت النصوص العربية المباشرة من طبقة الواجهة، وأصبح كل نص مرئياً أو وصف وصول يمر عبر `tr(messages, key)`.

**2. تشغيل `TripHistoryCard` فعلياً داخل «رحلاتي».** أزيلت البطاقة المحلية المكررة وربط المكوّن الإنتاجي الموجود: خريطة مصغرة، المسافة، المدة، التقييم، السائق، المركبة، السعر، التاريخ والحالة. التاريخ يستخدم اللغة النشطة بدلاً من `fr-DZ` المثبتة.

**3. تشغيل `HelpHub` فعلياً داخل «المساعدة».** أصبحت الشاشة تعرض آخر رحلة، الدردشة الفورية، اتصال الدعم والطوارئ، والأسئلة الشائعة المنشورة من إعدادات الخادم، مع إبقاء نموذج إنشاء التذكرة عبر `POST /support/tickets`.

**4. تعريب اختيار الوجهة والدرج بالكامل.** العنوان والبحث والموقع الحالي والمفضلة والأماكن الحديثة وحالات عدم وجود نتائج، إلى جانب أسماء أقسام الدرج وأوصاف الإغلاق وتغيير الصورة والمظهر، تتبع اللغة النشطة فوراً.

**5. إصلاح B-10 في التواريخ.** مرّرت اللغة النشطة إلى بطاقات الرحلات ورسائل تذاكر الدعم وطلبات حذف الحساب، وأزيل التنسيق الفرنسي المثبت من المحفظة.

**6. إكمال متطلب ماسح المحفظة.** أضيف إذن Android `CAMERA` إلى `app.json`، وأصبحت شاشة QR ورسائل رفض الإذن مترجمة. لا تزال عملية التحويل نفسها غير منفذة لأن الخادم لا يوفّر عقد تحويل محفظة إلى محفظة؛ لم يُخترع مسار API وهمي.

**التحقق:** صفر مفاتيح ترجمة ثابتة مفقودة · كل مفتاح جديد موجود 3/3 · صفر نص واجهة عربي مباشر في صفحات القائمة والدرج والوجهة · صفر TODO/FIXME/console · جميع ملفات المرحلة متوازنة وقابلة للتحليل · صفر مكتبات جديدة · صفر تغيير في عقود API.

---

### ✅ المرحلة 7 — التقسيم والأمان والتحقق النهائي

**ملفات جديدة (5):**

- `src/features/home/RouteComet.tsx`
- `src/features/home/NegotiationPanel.tsx`
- `src/features/home/TripPanel.tsx`
- `src/core/idempotency.ts`
- `RELEASE_SETUP.md`

**أهم الملفات المعدّلة:** `HomeScreen.tsx` · `storage.ts` · `session-store.ts` · `passenger-api.ts` · `TripPaymentScreen.tsx` · `eas.json` · `android/app/build.gradle`.

**1. تقسيم `HomeScreen`.** انخفض الملف من 2,298 إلى 1,849 سطراً. نُقلت حركة المسار إلى `RouteComet`، والمساومة وعدّاد صلاحية العرض إلى `NegotiationPanel`، وآلة حالة الرحلة الظاهرة إلى `TripPanel`. لم تتغير الخصائص أو العقود أو سلوك الشاشة.

**2. حماية المدفوعات من التكرار.** صار Checkout يرسل `Idempotency-Key`. يحتفظ `TripPaymentScreen` بالمفتاح نفسه عند فشل الشبكة أو إعادة الضغط، ولا ينشئ مفتاحاً جديداً إلا بعد نجاح العملية؛ وهذا يمنع خصماً مزدوجاً عند إعادة المحاولة.

**3. إخراج بيانات الجلسة من MMKV غير المشفر.** بقي MMKV للترجمة والثيم وبيانات الأصول غير الحساسة فقط. الهوية والملف الشخصي والتوكنات تُخزن الآن في `expo-secure-store` مع `AFTER_FIRST_UNLOCK_THIS_DEVICE_ONLY`، وتُحذف معاً عند انتهاء المصادقة.

**4. إزالة `/auth/me` غير الموثق.** استعادة الجلسة تستخدم الهوية الآمنة المخزنة و`GET /passenger/me` الموثق فقط. إذا غابت الهوية الآمنة تُمسح التوكنات بدلاً من إنشاء جلسة ناقصة.

**5. أسرار الخرائط والتوقيع.** حُذفت قيم Maps من `eas.json` وصار Production يتطلب إعداداً محمياً. أزيل توقيع Release بمفتاح Debug؛ يستخدم Gradle بيانات Keystore المحمية فقط. أُنشئ `RELEASE_SETUP.md` بأسماء الأسرار وخطوات Firebase والتوقيع دون تضمين قيم سرية.

**التحقق النهائي المتاح دون `node_modules`:** صفر `/auth/me` · `Idempotency-Key` موجود على Checkout · صفر مفاتيح Maps داخل `eas.json` · Release لا يشير إلى `signingConfigs.debug` · صفر TODO/FIXME/console · JSON صالح · كل ملفات TypeScript المعدلة قابلة للتحليل ومتوازنة.

**متطلبات خارجية قبل النشر:** تدوير مفاتيح Google القديمة واستبدال ملفات Firebase، إضافة أسرار EAS، توفير Keystore إنتاجي، حسم معرّف الحزمة النهائي، ثم تشغيل `npm install` وTypeScript/ESLint وبناء Preview. لا يمكن تنفيذ هذه الخطوات بأمان دون حسابات وأسرار المالك.

---

### ✅ إصلاح إعداد الخرائط وحدّ الانهيار — 30 يوليو 2026

**الملفات المعدّلة (3):**

| الملف | التعديل | السبب | الأثر |
| --- | --- | --- | --- |
| `src/components/AppErrorBoundary.tsx` | استبدال `BootScreen` بشاشة خطأ مترجمة مع زري إعادة المحاولة وإعادة التشغيل | كان أي خطأ React يظهر كشاشة إقلاع أبدية بلا تفسير | المستخدم يرى فشلاً قابلاً للتصرف؛ إعادة المحاولة تصفّر `failed`، وإعادة التشغيل تستخدم `expo-updates` مع تسجيل خروج آمن إذا تعذر التحديث |
| `src/core/i18n-fallback.ts` | إضافة 4 مفاتيح × 3 لغات لشاشة الخطأ | منع النصوص المثبّتة وضمان العمل دون شبكة | رسالة الإقلاع وأزرارها تتبع العربية والفرنسية والإنجليزية فوراً |
| `eas.json` | إزالة مفاتيح Maps القديمة المكتوبة حرفياً | القيم القديمة كانت مكشوفة ولا يجوز إعادتها إلى Git | المفتاح الجديد يُقرأ فقط من `GOOGLE_MAPS_ANDROID_API_KEY` في EAS Environment Variables |

**ثوابت محفوظة:** بقي `reportError(error, "react.render")` كما هو حرفياً · لم يُغيَّر أي مسار API أو عقد مع الخادم · لم تُضف أي مكتبة.

**التحقق:** `eas.json` صالح · صفر مفاتيح `AIza…` في `eas.json` و`app.config.js` · 12 ترجمة جديدة موجودة 3/3 · TypeScript قابل للتحليل · `reloadAsync()` موجود · إعادة المحاولة تضبط `failed: false`.

**مطلوب خارج الشيفرة:** تفعيل متغير EAS الجديد في `preview` و`production` أيضاً، ثم إنشاء بناء Android جديد. ملف `AndroidManifest.xml` الحالي مولّد قبل السر الجديد؛ لا تظهر `com.google.android.geo.API_KEY` فيه حتى إعادة البناء. إخفاقا بوابة الجودة المتبقيان خاصان بصور المركبات PNG الموجودة مسبقاً وغير مرتبطين بهذا الإصلاح.

---

_(يُحدَّث بعد كل مرحلة)_

# flaminGO — دفتر مواصفات بناء تطبيق الراكب (خطة تنفيذ للذكاء الاصطناعي)

> هذا الملف هو **مصدر الحقيقة** الذي يتبعه أي مساعد ذكاء اصطناعي لتحويل تطبيق
> الراكب الحالي إلى تطبيق عالمي المستوى على غرار Uber / inDrive / Yassir / Heetch،
> مع لمسة flaminGO الخاصة: **أبيض وأسود، وضع ليلي/نهاري، خفيف (<50MB)، سريع.**
>
> اقرأ هذا الملف بالكامل قبل تعديل أي شيء، ثم نفّذ المراحل بالترتيب.

---

## 0. قواعد العمل الإلزامية (اقرأها أولاً)

1. **اقرأ قبل أن تكتب:** قبل تعديل أي ملف، افتحه واقرأه بالكامل. لا تخمّن
   محتوى ملف أو توقيع دالة.
2. **لا تكسر ما يعمل:** التطبيق يعمل حالياً (المصادقة، الخريطة، الطلب، التتبّع
   اللحظي). أي تعديل يجب أن يبقي هذه المسارات تعمل.
3. **بيئة بلا إنترنت:** لا يمكن تشغيل `npm install` أو `eas` أو `expo prebuild`
   داخل صندوق العمل. تحقّق من صحة TypeScript صياغياً فقط عبر مترجم
   `typescript` المستقل. لا تضف أي تبعية جديدة إلا إذا كانت **موجودة مسبقاً**
   في `node_modules` (راجع `package.json`).
4. **بعد كل مجموعة تعديلات:** افحص الصياغة، ثم أعد التغليف إلى
   `flaminGO-passenger-fixed.zip` (باستثناء `node_modules/.git/.expo`).
5. **التغييرات الأصلية (native) تتطلب إعادة بناء EAS** ولا تظهر عبر OTA:
   الأيقونات، الأذونات، مفاتيح الخرائط، إزالة/إضافة مكتبات أصلية، شاشة الإقلاع.
6. **اللغة فقط لا الاتجاه:** ممنوع استدعاء `I18nManager.forceRTL`. التطبيق يبقى
   LTR دائماً؛ تبديل اللغة يبدّل النصوص فقط.
7. **الهوية البصرية:** أبيض/أسود فقط + رمادي محايد + الذهبي `#D9A520` كلمسة
   نادرة (شعار/تفاصيل صغيرة فقط). لا ألوان أخرى.

---

## 1. خريطة المشروع الحالية (ابدأ بقراءة هذه الملفات)

```
src/
  App.tsx                      نقطة التركيب: مزوّدات (QueryClient/SafeArea) + التنقل
  index.js (بالجذر)            نقطة دخول Expo
  navigation/
    RootNavigator.tsx          مكدّس التنقل (Auth/Profile/Home/Menu/الخدمات)
    types.ts                   RootStackParamList — أسماء الشاشات وبارامتراتها
  core/
    api.ts                     محور Axios؛ يقرأ EXPO_PUBLIC_API_URL (يرمي إن غاب)
    passenger-api.ts           خدمات الراكب (config/paymentMethods/navigation...)
    contracts.ts               كل الأنواع: Locale, Profile, Trip, Quote, VehicleType...
    i18n.ts                    loadTranslations / tr / merge (fallback ثم الخادم)
    i18n-fallback.ts           FALLBACK_BUNDLES{ar,fr,en} + fallbackFor + humanizeKey
    locale-store.ts            مخزن اللغة (zustand): locale/messages/setLocale/hydrate
    use-messages.ts            خطّاف useMessages() ← يقرأ من locale-store
    session-store.ts           الجلسة والملف الشخصي (restore/accept/logout)
    storage.ts                 cache = MMKV + SecureStore للتوكنات
    assets.ts                  مزامنة الأصول المُدارة (syncManagedAssets/managedAsset)
    notifications.ts           تسجيل/إلغاء الإشعارات
    observability.ts           reportError
  features/
    auth/AuthScreen.tsx        الدخول بالهاتف + مبدّل اللغة بالأعلام (تم)
    auth/firebase.ts           requestPhone/confirmPhone/completeEmailLink/exchange
    profile/ProfileScreen.tsx  إكمال الملف: الاسم + الجنس (الصورة أُزيلت)
    home/HomeScreen.tsx        الخريطة + البحث + الطلب + التتبّع (الشاشة الأكبر)
    menu/MenuScreen.tsx        القائمة الجانبية (عناصر من الخادم)
    services/*.tsx            شاشات ثانوية (المحفظة/الإشعارات/الرحلات/الدعم...)
    trip/realtime.ts           اتصال WebSocket لتتبّع الرحلة (connectTrip)
    trip/trip-api.ts           passengerApi: catalog/autocomplete/directions/offers...
  components/
    PassengerScreen.tsx        Screen/Loading/Message/ui (أنماط مشتركة) ← مهم للثيم
    AppErrorBoundary.tsx
    BootScreen.tsx
```

**أوامر البدء الإلزامية للـ AI:** اقرأ بالترتيب: `contracts.ts` → `api.ts` →
`passenger-api.ts` → `trip/trip-api.ts` → `navigation/types.ts` →
`components/PassengerScreen.tsx` → `home/HomeScreen.tsx` بالكامل. لا تبدأ
التعديل قبل استيعاب هذه السبعة.

---

## 2. نظام الثيم (أبيض/أسود + ليلي/نهاري) — الأساس لكل شيء

أنشئ `src/core/theme.ts` كمصدر وحيد للألوان، ومخزن ثيم `src/core/theme-store.ts`.

### 2.1 لوحة الألوان (رمزية فقط — لا ألوان صريحة في الشاشات بعد اليوم)
```ts
export type ThemeName = "light" | "dark";
export type Palette = {
  bg: string;        // الخلفية الأساسية
  surface: string;   // البطاقات/الأوراق
  surfaceAlt: string;// حقول الإدخال
  text: string;      // النص الأساسي
  textMuted: string; // النص الثانوي
  border: string;    // الحدود/الفواصل
  primary: string;   // زر أساسي (أسود في النهاري، أبيض في الليلي)
  onPrimary: string; // نص فوق الزر الأساسي
  accent: string;    // "#D9A520" الذهبي — لمسة نادرة فقط
  danger: string;    // "#B42318"
  mapStyle: "standard" | "night"; // نمط الخريطة
};
export const LIGHT: Palette = {
  bg: "#FFFFFF", surface: "#FFFFFF", surfaceAlt: "#F4F4F5",
  text: "#0E0E10", textMuted: "#6B7280", border: "#ECECEC",
  primary: "#0E0E10", onPrimary: "#FFFFFF", accent: "#D9A520",
  danger: "#B42318", mapStyle: "standard",
};
export const DARK: Palette = {
  bg: "#0B0B0C", surface: "#151517", surfaceAlt: "#1E1E22",
  text: "#F5F5F6", textMuted: "#9AA0A9", border: "#26262B",
  primary: "#FFFFFF", onPrimary: "#0B0B0C", accent: "#D9A520",
  danger: "#F97066", mapStyle: "night",
};
```

### 2.2 مخزن الثيم (zustand)
- المفتاح المخزَّن: `app.theme` بقيمة `"light" | "dark" | "system"`.
- الافتراضي `"system"` (يتبع `Appearance.getColorScheme()` مع مستمع تغيّر).
- الصادرات: `useTheme()` تُرجع `{ palette, name, mode, setMode }`.
- زر التبديل في القائمة (menu): نهاري / ليلي / تلقائي (النظام).

### 2.3 قاعدة تطبيق الثيم
- كل شاشة تستدعي `const { palette } = useTheme()` وتبني أنماطها من `palette`.
- امنع الألوان الحرفية: ابحث عن أي `#RRGGBB` في `src/features/**` واستبدلها
  بمفاتيح `palette`. الاستثناء ال��حيد: الشعار والأصول الصورية.
- خريطة `react-native-maps`: مرّر `customMapStyle={palette.mapStyle === "night"
  ? NIGHT_MAP_JSON : []}` وأنشئ `NIGHT_MAP_JSON` (نمط خريطة أسود/رمادي بسيط).
- شريط الحالة: `<StatusBar style={name === "dark" ? "light" : "dark"} />`.
- ابدأ التطبيق بـ `ThemeProvider` أعلى `RootNavigator` في `App.tsx`، ومرّر
  ثيم `@react-navigation` (DarkTheme/DefaultTheme) بألوان `palette`.

---

## 2ت. مرجع بصري مُستخلَص من لقطات Heetch (تم تحليلها فعلياً)

تمّ استرجاع لقطتين حقيقيتين من تطبيق Heetch وتحليلهما. الملامح التصميمية
الإلزامية المستخلصة (طبّقها بلمستنا: أبيض/أسود + ذهبي بدل الوردي):

**أ) شاشة تأكيد رمز الدخول (OTP):**
- خلفية سوداء صرفة، وعنوان ضخم جداً بأحرف كبيرة (UPPERCASE) ووزن خط ثقيل جداً
  (800–900) — هذه **الهوية الطباعية الأساسية**، اعتمدها في كل العناوين.
- حقول الرمز على شكل **شُرَط أفقية قصيرة** (dashes) وليس مربّعات — مينيمالي.
- نص ثانوي رمادي، روابط بيضاء تحتها خط، ورابط سفلي بلون اللمسة (عندنا: ذهبي).

**ب) الشاشة الرئيسية (الخريطة):**
- **خريطة داكنة كامل الشاشة** في الوضع الليلي (شوارع سوداء، خلفية كحلي/رمادي قاتم)
  — يطابق `NIGHT_MAP_JSON`.
- **زر قائمة دائري عائم** (☰) في أعلى الزاوية، بخلفية دائرية داكنة شبه شفافة.
- نقطة الموقع الحالي زرقاء بسهم اتجاه.
- **ورقة سفلية** تبدأ من أسفل الشاشة (مكان صندوق البحث).
- دبابيس أماكن بأيقونات دائرية رمادية محايدة.

**قاعدة التحويل للمستنا:** كل ما هو وردي عند Heetch → ذهبي `#D9A520` عندنا (لمسة نادرة)،
والباقي أبيض/أسود صرف حسب الثيم.

---

## 3. شاشة الخريطة على طراز Heetch (أهم مرحلة)

المرجع: تطبيق Heetch — خريطة كامل الشاشة + ورقة سفلية (bottom sheet) بسيطة.
اعمل داخل `home/HomeScreen.tsx` (يحتوي مسبقاً على `pickup/destination/
searchTarget/search` والاستعلامات). أعد ترتيب طبقة العرض فقط دون كسر المنطق.

### 3.1 التخطيط
```
┌─────────────────────────────┐
│  ☰ (قائمة)         ◐ (ثيم)   │  ← أزرار عائمة أعلى الخريطة
│                             │
│         MapView             │  ← كامل الشاشة، خلف كل شيء
│      (دبوس المنتصف)          │
│                             │
├─────────────────────────────┤
│  ● من: [نقطة الانطلاق]  ✎    │  ← الورقة السفلية (bottom sheet)
│  ◆ إلى: [الوجهة]            │
│  [ التالي / اطلب flaminGO ]  │
└─────────────────────────────┘
```

### 3.2 السلوك المطلوب (مطابقة Heetch)
1. **صندوق البحث في الأسفل** داخل ورقة سفلية بحواف علوية دائرية، ترتفع عند
   التركيز لعرض نتائج الإكمال التلقائي (autocomplete) في قائمة قابلة للتمرير.
2. **نقطة الانطلاق قابلة للتحرير:** حقل "من" مستقل. عند الضغط عليه
   `setSearchTarget("pickup")` ويكتب المستخدم أي مكان للانطلاق (ليس شرطاً
   موقعه الحالي). الموقع الحالي يبقى فقط كـ **اقتراح افتراضي أول** في القائمة
   ("موقعي الحالي") وليس إلزامياً.
3. حقلان: **من (pickup)** و **إلى (destination)**، كلاهما يفتح نفس ورقة البحث
   مع تبديل `searchTarget`.
4. اختيار اقتراح يملأ النقطة المناسبة، ثم تتوسّط الخريطة تلقائياً
   (`fitToCoordinates` موجود مسبقاً).
5. بعد تحديد النقطتين: تظهر بطاقة **فئات المركبات + السعر + طريقة الدفع + زر
   الطلب** داخل نفس الورقة (استخدم منطق `catalog/quote/payment` الموجود).
6. حالة الرحلة النشطة (SEARCHING/ACCEPTED/ARRIVING/IN_PROGRESS): تتحوّل الورقة
   إلى بطاقة حالة السائق مع التتبّع اللحظي (المنطق موجود في `connectTrip`).
7. **دبوس منتصف الخريطة** خيار بديل لتحديد النقطة (اسحب الخريطة → العنوان يُحدَّث
   عبر `reverse geocode`)، مثل Uber/Heetch. اجعله اختيارياً للنقطة النشطة.

### 3.3 قواعد تقنية
- استخدم `react-native-reanimated` (موجود) لحركة الورقة. لا تُضِف مكتبة
  bottom-sheet جديدة إن لم تكن مثبّتة — ابنِ الورقة يدوياً بـ `Animated`.
- كل النصوص عبر `tr(messages, key)`؛ أضف المفاتيح الناقصة إلى الحُزم الثلاث.
- كل الألوان من `palette` (الوضع الليلي/النهاري).

---

## 4. الترجمة (رئيسية داخل التطبيق / ثانوية من الخادم) — قائم، أكمِله

- الرئيسية مضمّنة في `i18n-fallback.ts` (`ar/fr/en`)؛ الثانوية من
  `/translations/public/{locale}` وتُدمج فوق الرئيسية.
- عند إضافة أي نص جديد في الواجهة: أضف مفتاحه إلى الحُزم الثلاث في
  `i18n-fallback.ts`. ممنوع نص حرفي في الشاشات.
- مبدّل اللغة بالأعلام موجود في `AuthScreen`؛ أضِف مبدّلاً مماثلاً (أو من القائمة)
  ليتوفّر بعد الدخول أيضاً.
- **إلزامي:** لا `forceRTL`. الاتجاه LTR ثابت.

---

## 5. إزالة الصورة الشخصية (مُنفَّذ — حافظ عليه)

- أُزيلت من `ProfileScreen` (الاسم + الجنس فقط) ومن `MenuScreen` (استُبدلت بدائرة
  تحمل الحرف الأول). ستُعتمد الصور لاحقاً في **تطبيق السائق فقط**.
- لا تُعِد أي عنصر رفع صور في تطبيق الراكب. أبقِ الخادم يقبل
  `avatarUrl` اختيارياً دون إجبار.

---

## 6. الشعار عند الدخول (مُنفَّذ — حافظ عليه)

- أُزيلت الدائرة الرمادية خلف الشعار (`heroGlow`) وأصبح الشعار يظهر فوراً
  (`require` مجمّع، بلا انتظار `assetsReady`).
- شاشة الإقلاع (splash) تبقى `brand-master-logo.png` كما هي.

---

## 7. القائمة (Menu) — أعِد تصميمها

- رأس: دائرة الحرف الأول + الاسم + الهاتف (بلا صورة).
- مجموعات مرتّبة: **الحساب**، **الرحلات والأماكن**، **المدفوعات والمحفظة**،
  **الإشعارات**، **الدعم والقانوني**، ثم **الإعدادات** (اللغة + الثيم
  ليلي/نهاري/تلقائي)، وأخيراً **تسجيل الخروج** بلون `danger`.
- العناصر تأتي من الخادم (`passenger.navigation`)؛ أضِف قسم الإعدادات محلياً.
- كل الألوان من `palette`.

---

## 8. خفّة التطبيق (<50MB) — قائمة تحقّق

مُنفَّذ: حذف `three` و`expo-gl` و`@types/three` وملف `.glb`؛ تفعيل
`enableProguardInReleaseBuilds` و`enableShrinkResourcesInReleaseBuilds`.

أكمِل:
1. **بناء AAB** لا APK للإنتاج (مضبوط: `production.android.buildType =
   app-bundle`). يوزّع Google Play نسخة لكل معمارية/كثافة → أصغر بكثير من الـ150MB
   (كانت APK شاملة لكل المعماريات).
2. راجع كل تبعية في `package.json` واحذف غير المستخدم فعلياً (تحقّق بـ grep).
3. ضغط الأصول: أبقِ صور PNG بحجم معقول (أيقونات ≤1024²، صور ≤ الحاجة). لا تُضِف
   خطوطاً أو صوراً كبيرة.
4. لا تُضِف مكتبات ثقيلة (رسوم ثلاثية الأبعاد، فيديو، رسوم متحركة ضخمة).
5. تأكّد أن `assetBundlePatterns` لا يحزم أصولاً غير مستخدمة.
6. الهدف: APK لكل معمارية أو AAB نهائي < 50MB. تحقّق بعد بناء EAS.

---

## 9. معايير القبول (Definition of Done)

- [ ] ثيم ليلي/نهاري/تلقائي يعمل على كل الشاشات، بألوان من `palette` فقط.
- [ ] لا أي `#RRGGBB` حرفي في `src/features/**` (عدا الأصول الصورية).
- [ ] الخريطة على طراز Heetch: ورقة بحث سفلية + نقطة انطلاق قابلة للكتابة.
- [ ] تبديل اللغة يغيّر النص فقط، بلا قلب اتجاه.
- [ ] لا أثر لأي رفع/عرض صورة شخصية في تطبيق الراكب.
- [ ] الشعار يظهر فوراً بلا دائرة رمادية.
- [ ] `EXPO_PUBLIC_API_URL` مضبوط في `eas.json` لكل ملفات البناء.
- [ ] فحص صياغة TypeScript يمرّ لكل الملفات المعدّلة.
- [ ] بناء EAS ينتج AAB والتطبيق يفتح دون انهيار والحجم النهائي < 50MB.

---

## 10. أوامر التحقّق (بلا إنترنت)

- فحص الصياغة: استخدم مترجم `typescript` المستقل عبر
  `ts.transpileModule` على كل ملف معدّل (فئة التشخيص = 1 تعني خطأ صياغة).
- تحقّق JSON: `JSON.parse` لِ�� `app.json` و`eas.json` و`package.json`.
- إعادة التغليف:
  `zip -rq flaminGO-passenger-fixed.zip . -x 'node_modules/*' '.git/*' '.expo/*'`
- البناء النهائي (على جهاز المستخدم، بإنترنت):
  `eas build --platform android --profile production`

---

## 11. سجل ما أُنجز فعلاً حتى الآن (لا تُعِد عمله)

- إزالة الرسم ثلاثي الأبعاد → شعار، الصورة الافتراضية، معرض فقط بلا سيلفي.
- تلوين الهوية: flamin فحمي + GO ذهبي.
- مبدّل اللغة بالأعلام (الجزائر/فرنسا/بريطانيا) على شاشة الدخول، أيقونات فقط.
- ترجمة رئيسية مضمّنة (ar/fr/en) + ثانوية من الخادم.
- تبديل اللغة بلا قلب اتجاه (إزالة `forceRTL`).
- إزالة الدائرة الرمادية + ظهور الشعار فوراً.
- إزالة الصورة الشخصية من الراكب (ProfileScreen + MenuScreen).
- أيقونة التطبيق = شعار flaminGO (عادية + متكيّفة داخل منطقة الأمان).
- إصلاح الانهيار: `EXPO_PUBLIC_API_URL` في `eas.json` لكل الملفات.
- تفعيل Proguard/Shrink + بناء AAB للإنتاج.
- (خادم) اعتماد GCS عبر بيانات حساب الخدمة على Render.

**التالي (لهذا الملف):** المرحلتان 2 (نظام الثيم) و3 (خريطة Heetch) هما
أكبر عملين متبقّيين ويجب تنفيذهما بعناية مع اختبار على جهاز حقيقي.
